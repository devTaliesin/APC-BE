import { CreateVideoSourceDto } from './create-video-source.dto';

describe('CreateVideoSourceDto', () => {
  it('should be defined', () => {
    expect(new CreateVideoSourceDto()).toBeDefined();
  });
});
